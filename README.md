# Syslab-Drone-Navigation-Gesture-Based-ML

This contains all of the Training and Testing images, however we split up the Training data, so in order to rebuild it, you are required to do this command in gitbash after going to the repo that contains the parts.
cat Testing_images.zip.part_* > Testing_images_REBUILT.zip
